{
  "name": "fund-price-forecast-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "recompute:all": "node --import tsx ./scripts/recompute-all.ts",
    "source:check": "node --import tsx ./scripts/source-check.ts",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.901.0",
    "@neondatabase/serverless": "^0.10.4",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
